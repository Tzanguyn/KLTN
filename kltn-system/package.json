{
  "name": "kltn-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "test": "npm run test:integration --workspace backend",
    "db:setup": "npm run prisma:generate --workspace backend && npm run prisma:migrate --workspace backend && npm run prisma:seed --workspace backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.63.3",
    "concurrently": "^9.1.2"
  }
}
